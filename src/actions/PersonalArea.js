import { createAction } from 'redux-actions';

export const signIn = createAction('[PersonalArea] SignIn')
export const add = createAction('[Personal Area], Add Contact')
export const remove = createAction('[Personal Area], Remove Contact')